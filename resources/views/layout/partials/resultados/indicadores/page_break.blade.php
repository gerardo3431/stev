{{-- @if (count($estudios ) === 1 && count($estudios[0]->analitos) < 8)

@else
    @if ($contadorArea !== $totalAreas)
        <div class="break"></div>
    @endif
@endif
@php
    $contadorArea++;
@endphp --}}
